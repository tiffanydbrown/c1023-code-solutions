/* exported getAverageOfThreeNumbers */
function getAverageOfThreeNumbers(x, y, z) {
  const average = (x + y + z) / 3;
  return average;
}

console.log(
  'Value of getAverageOfThreeNumbers:',
  getAverageOfThreeNumbers(5, 6, 7)
);
