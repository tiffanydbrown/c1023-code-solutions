# typescript-objects-quiz-notes

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

## Quiz Questions

After completing this exercise, you should be able to discuss or answer the following questions:

- What are objects used for?
  Objects are used for storing large amounts of data, or data that have certain characteristics that we want to use more than once.

- What are object properties?
  Object properties are key:value pairs.

- Describe object literal notation.
  Object literals notation: keyword variable = {key:value};

- How do you remove a property from an object?
  You can remove properties from an object with the delete operator.

- What are the two ways to get or update the value of a property?
  You can get or update the value of a property with either dot or bracket notation.

- What is the purpose of interfaces in TypeScript?
  The purpose of interfaces in TypeScript id yo build up new types from other types by extending them.

- How do optional properties in a TypeScript interface differ from mandatory properties, and why are they useful?
  Optional properties differ from mandatory properties by denoting a ? at the end of the property name in the declaration. This allows you
  to describe possibly available properties.

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
